remotes:
  - git_url: https://github.com/FelipeFuhr/ffreis-platform-standards
    ref: v1.6.0
    configs:
      - lefthook/base.yml

pre-commit:
  parallel: true
  commands:
    markdownlint:
      glob: "commands/*.md"
      run: markdownlint {staged_files}

pre-push:
  parallel: false
  commands:
    test:
      run: make test
